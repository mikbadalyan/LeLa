This folder mirrors the PWA SVG icon set for easy replacement.

The app reads icons from:

- `frontend/public/icon/*.svg`

If you replace an icon here, also replace the same file in `frontend/public/icon/`.
